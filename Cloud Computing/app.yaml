runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app

runtime_config:
  operating_system: "ubuntu22"

#[START gae_flex_storage_yaml]
env_variables:
  CLOUD_STORAGE_BUCKET: mendaurbeta
#[END gae_flex_storage_yaml]
